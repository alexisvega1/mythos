---
description: Cursor lane — CI, tooling, dashboards; never serve/router (Claude Code)
alwaysApply: true
---

# Cursor lane (mythos-cursor worktree)

**Checkout:** `/Users/alexisvega/mythos-cursor` only. **Branches:** `cur/*`.

## Do NOT touch — Claude Code owns these

**Lane A** (`/Users/alexisvega/mythos` or `mythos-laneB` · `cc/*`):

- `src/mythos/data/**`, `train.py`, `model/gpt.py`, `checkpoint.py`
- `src/mythos/eval/harness.py`, `eval/composite.py`
- `tests/regression/test_no_fake_wins.py`, `data/corpus/**`, `data/fixtures/**`

**Lane B serve/router** (`/Users/alexisvega/mythos-laneB` · `cc/serve-real` and follow-ons):

- `src/mythos/serve/**` (inference, api, engine loading)
- `src/mythos/router/**` (dual-tier safety router)

Claude Code is actively implementing real checkpoint-backed serving. **Do not edit,
rebase over, or PR changes to these paths.**

**Lane C** (Codex · `cdx/*`): `posttrain.py`, `agents/swe/**`, `agents/secsec/**`

## Cursor owns

- `.github/workflows/**` — CI matrix, no-fake-wins gate, smoke jobs
- `scripts/**` — worktree bootstrap, cloud bootstrap (when added)
- `eval/dashboards/**` — results.tsv dashboard
- `src/mythos/lab.py`, `src/mythos/cli.py` — only if not conflicting with CC
- README / docs polish (not `PLAN.md`, `SECURITY.md`, `program.md`)

Consume checkpoint + RawScores contracts from `docs/AGENT_LANES.md`; never redefine.

Never commit in `/Users/alexisvega/mythos-laneB` or `/Users/alexisvega/mythos` on `cc/*`.
